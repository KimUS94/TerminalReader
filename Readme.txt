# First Test Code

# git push -u --force tmr master
# rm -rf .git


# git init
# git add .
# git commit -m "message"
# git log
# git remote add <name> <url>
# git push -u --force <name> master

# got remote -v
